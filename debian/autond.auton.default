# defaults file for auton

# Service can be enabled by setting
# AUTON_ENABLE="yes"
AUTON_ENABLE="yes"
ARGS="-l info"

if [ -f /usr/share/autond/envfile ];
then
    set -a
    . /usr/share/autond/envfile
    set +a
fi
